/**
 * Image reference parser stub.
 * Returns empty array for non-WhatsApp channels.
 */
export function parseImageReferences(
  _messages: unknown[],
): Array<{ relativePath: string; mediaType: string }> {
  return [];
}
